# CAC Art Lab 开发日志

本日志于 2026-09-11 首次整理。前两段历史记录依据已有交接记录、实际文件和 Git 历史事后整理，不是当日现场日志；无法确认的耗时不补写。Git 提交时间按记录中的 UTC−04:00 时区列出。

## 2026-09-10 — 建立仓库（事后整理）

完成了：建立 `cac-art-lab` 仓库，加入 `.gitattributes`、`.gitignore` 和初始 `README.md`。Git 中有真实提交 `53d1f4a`，时间为 19:30:40，说明为 `Initial commit`。

遇到的问题：现有记录不足以确认这一阶段遇到的具体报错。

检查方法：2026-09-11 整理日志时读取了实际 Git 历史。该核对是本次事后检查，不是对当时操作过程的补记。

最后怎么修好：本阶段没有足够记录可写具体修复。

核对结果：初始提交存在；该提交不包含完整 M1 成果。

使用的工具：GitHub Desktop 用于建仓、提交和上传；实际保存的修改可通过 Git History 查看。

耗时：未知。

下一步（当时）：整理已有脚本和原始数据，导出基础 JSON。

## 2026-09-11 — 抽样、基础导出与数据入仓（事后整理）

完成了：整理 `Data/ReadData.py` 与根目录 `web/app_data.json`，导出固定抽样的 60 条基础记录；将原始研究数据加入仓库。真实 Git 提交为：

| 时间 | 提交 | 说明 |
|---|---|---|
| 08:49:35 | `8e51875` | `Add CAC data and classroom script` |
| 08:53:41 | `09ffaa8` | `Add remaining original data` |

遇到的问题：交接记录记载，曾经抽样到 `selected` 后仍用 `for pair in pairs`，因此继续处理全部 594 对；区间统计曾使用 `model_q_A`；还出现过输出到 `Data/web/` 的路径问题，以及复制文件后多套一层 `CAC/Data/`、`CAC/web/` 的目录问题。

检查方法：历史运行反馈记录基础输出在 N=60 时为 60 条、裁判区间内 5 条，N=40 时为 40 条、区间内 3 条。2026-09-11 本次又核对了接手代码与 Git：代码使用 `for pair in selected`，区间条件为 `0.4 <= obj["q_A"] <= 0.6`，`WEB=BASE.parent/"web"`，数据和输出均位于仓库根目录下对应的 `Data/` 与 `web/`。

最后怎么修好：历史中已把循环改为遍历 `selected`，把统计字段由 `model_q_A` 改为 `q_A`，修正输出路径并把 `Data/`、`web/` 移至正确层级。这些修正在本次接手前已经存在，不能写成本次新修复。

核对结果：历史反馈支持基础导出的 60/5 和 40/3，最终基础版本恢复 N=60。它们不是特征、缩略图及检查器的完整 M1 验收记录。接手时仍缺少 `m1_tasks.py`、检查器、缩略图、环境依赖记录和完整文档。

数据处理使用读取 JSON、列表／字典、索引关联和固定抽样。本项目的数据文件包括画作信息、裁判投票、特征和模型预测；画作图片来源见 Data/README.md。

耗时：未知。

下一步（当时）：取得配套文件，再补齐 M1 的特征、图片、正式复现检查及说明。

## 2026-09-11 — 核对作业包并补齐 M1（本次实测记录）

完成了：本次在 Downloads 中找到了 `CAC_第一课作业包`，读取了作业单及配套的 TODO 骨架、图片示例、接入说明、检查器和 README／开发日志模板，补上交接记录中原件未找到的缺口。检查器按收到的配套文件保留。已据此补齐两个 TODO 和主脚本调用，整理根目录 `README.md` 与本日志；保留原始 `Data/README.md`。正式生成与检查已完成。

遇到的问题：接手基础版本缺少完整 M1 的特征计算与缩略图输出，也没有检查器的真实复现记录。旧交接未附完整配套文件，需要先取得并核对作业包。

检查方法：将接手文件、交接记录和配套文件相互核对。规定的处理规则为全部 28 个特征、使用全部 300 幅画的极差、A−B、极差为零时输出 0、按绝对值降序且同值按名称升序；图片示例明确使用 LANCZOS、JPEG quality=88、保比例且长边不超过 400。

最后怎么修好：补齐 `feature_ranges()` 与 `top_differences()` 两个 TODO，在原有 `ReadData.py` 结构中接入特征和缩略图处理，并执行运行检查、整理文档。核对确认 `check_m1.py` 与原配套文件逐字节相同，`make_thumbnail()` 与原配套文件的代码结构相同。

核对结果：项目虚拟环境实测为 Python 3.9.6、Pillow 11.3.0，本日志按实际运行环境记录。合成数据检查了零极差、正负差值、同值名称排序和 `limit`；首对画产生 28 条差值，top 3 与作业单核对值一致。单张缩略图由 600×432 缩为 400×288，并已查看图像完整性。临时副本先通过首轮 N=60 检查，再将核对后的代码写回桌面项目仓库执行下面的正式验收。

正式验收于 2026-09-11 10:37:01–10:37:04（UTC−04:00）在桌面项目仓库执行。顺序为 N=60→40→60，每个设置都重新生成并检查两轮，共运行生成器 6 次、原配套检查器 6 次。下表为真实输出：

| 阶段 | 实际 JSON 条数 | 实际 `q_A` 区间条数 | 实际引用图片数 | 检查与复现结果 |
|---|---|---|---|---|
| 第一次 N=60 | 60 | 5 | 106 | 首轮通过并建指纹，第二轮复现通过 |
| N=40 | 40 | 3 | 75 | 首轮通过并建指纹，第二轮复现通过 |
| 最终 N=60 | 60 | 5 | 106 | 两轮均通过，复现通过 |

实测证据：`verification.txt` 由原配套检查器自动追加，含 N=60 三条与 N=40 一条复现通过记录。恢复 60 后已有指纹，两次检查都会追加，这是检查器的正常行为。N=60 输出指纹为 `6efdccdddc86cb4ddfc1e154c2785aebe2d61d076f773ede1497df3b0e61e859`；N=40 为 `fb423ff18fcfedf46c937f7e6991e6d35ac741debd8b2c145ad22c0646926cce`。

补充检查：最终代码为 N=60，JSON 有 60 条，每条 3 个 top_features；106 张 JPEG 均可完整解码，尺寸和比例通过原配套检查器。全部 313 个原始数据文件（包括 300 张原图和 Data/README.md）的 SHA-256 与接手时一致。未修改原配套检查器，未手写 PASS 或验收记录。N=40 阶段保留已有缩略图，没有删图凑数。

运行环境问题：首次安装 Pillow 时，受限网络返回域名解析失败；获准联网后安装成功。这是本次运行环境问题。项目环境使用实际创建并运行的 Python 3.9.6。

提交记录：本次代码、检查工具、依赖、JSON 和 106 张缩略图已于 10:38:27（UTC−04:00）提交为 `cc73cfe`（`Add feature differences and thumbnails`），并成功推送至现有远端 main。说明、开发日志和真实验收记录作为后续文档提交保存；具体提交编号见 Git History。

未完成的交付项：桌面控制工具因安全策略拒绝访问 Terminal，尚未取得最终 N=60 的终端截图；没有制作替代或模拟截图。

依赖：Pillow 提供图片处理功能，版本固定在 requirements.txt。配套检查工具按原文件保留。

耗时：未记录。

下一步：保存最终 N=60 的真实终端截图。网页交互、部署和赛事提交仍是后续工作；当前代码与文档的提交以 Git History 为准。
